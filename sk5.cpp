#include <stdio.h>
#include <string.h>

int main() {
    char str[100], temp;
    int i, length;

    printf("Enter a string: ");
    scanf("%s", str);

    length = ; // use the library function in the string

    for (i = 0; i < len / 2; i++) {
        temp = str[i];
        str[i] = ________; // Swap characters
        str[length - i - 1] = temp;
    }

     //print the reversed string
    return 0;
}

