#include <stdio.h>

int main() {
    int num, fact = 1, i;

    printf("Enter a number: ");
    scanf("%d", &num);

    for () //write the condition for loop
	{
        fact = ________; // complete the the code
    }

    printf("Factorial of %d is %d\n", num, fact);
    return 0;
}

