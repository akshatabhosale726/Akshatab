#include <stdio.h>

int main() {
    char str[100], temp;
    int i, j;
    
    printf("Enter a string: ");
    gets(_________); // Fill in the blank

    for (//complete for loop*/);  // Find length

    for (i = 0, j = j - 1; i < j; i++, j--) {
        /*complete the code*/
    }
    
    printf("Reversed string: %s\n", str);
    return 0;
}

