#include <stdio.h>

int sumOfSquares(int arr[], int n);

int main() {
    int arr[] = {1, 2, 3, 4};
    int n = 4;
    printf("Sum of squares: %d\n", sumOfSquares(arr, n));
    return 0;
}

