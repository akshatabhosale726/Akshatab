#include <stdio.h>

// Function prototype
int largest(_________, _________, _________); // Fill in the blanks

int main() {
    int a, b, c;
    printf("Enter three numbers: ");
    scanf("%d %d %d", &a, &b, &c);
    
    printf("Largest number is %d\n", largest(a, b, c));
    return 0;
}

// Function definition
int largest() // accept the parameters
{
   //write the code for finding largest number
}

